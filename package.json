{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "clean": "rm -rf node_modules packages/**/node_modules packages/**/dist packages/**/tmp"
  }
}
